repos:
  - repo: local
    hooks:
      - id: spec-lint
        name: Validate SSVC spec registry
        entry: ssvc-spec-lint
        language: system
        types: [yaml]
        files: ^specs/
        pass_filenames: false
